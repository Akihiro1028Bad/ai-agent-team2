polling_interval_sec: 30

accounts:
  default:
    token_command: "gh auth token"
    default: true

repositories:
  - owner: "Akihiro1028Bad"
    repo: "ai-agent-team2-test"
    account: "default"
    label: "ai-agent"
    base_branch: "main"

concurrency:
  max_total: 1
  max_per_repo: 1

timeouts:
  hearing_timeout_hours: 24
  phase_timeout_sec: 600

slack:
  webhook_url: "https://hooks.slack.com/services/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"
